import random
class rng():

    villanDict = {}

    def generateAttatFrequency(name,platform):
        global villanDict

        villanDict[name]
        villanDict[name]='';


    def generateBluffFrequency(name,platform):
        global villanDict

        villanDict[name]
        villanDict[name]='';


    def generateBluffCatchFrequency(name,platform):
        global villanDict

        villanDict[name]
        villanDict[name]='';

    def generate(self):

        random_number = random.randint(1, 100)
        print(random_number)
